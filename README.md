# Breaking-RSA-TryHackMe-Writeup
Recovery of an RSA private key via Fermat factorization after discovering an exposed id_rsa.pub on a web directory; used the recovered key to authenticate and retrieve the flag.
